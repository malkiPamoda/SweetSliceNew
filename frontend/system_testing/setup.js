import { beforeEach, afterEach } from 'vitest';

beforeEach(async () => {
  // Setup code before each test (e.g., initialize database)
});

afterEach(async () => {
  // Cleanup code after each test (e.g., reset database)
});